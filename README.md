# Tistory Image Archive

Content-hash image archive for long-lived Tistory post images.

- Filenames are SHA-256 based to keep URLs immutable.
- Intended public URL format: `https://bmoh95.github.io/tistory-image-archive/images/<sha>.<ext>`.
- Do not delete or rewrite existing files; add new hashes only.
